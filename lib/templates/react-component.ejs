import React, { memo } from 'react';
import {<%= data.styleName %>} from './style'

  const <%= data.name %> = memo(() => {
    return (
    <<%= data.styleName %>>
      <%= data.name %>
    </<%= data.styleName %>>
    )
    })
    export default <%= data.name %>